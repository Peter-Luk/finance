export ZSH="/home/peterluk/.oh-my-zsh"
ZSH_THEME="robbyrussell"
DISABLE_MAGIC_FUNCTIONS="true"
ENABLE_CORRECTION="true"
COMPLETION_WAITING_DOTS="true"
HIST_STAMP="dd/mm/yyyy"

plugins=(git sudo vi-mode tmux)
ZSH_TMUX_AUTOSTART=true
ZSH_TMUX_AUTOSTART_ONCE=true
ZSH_TMUX_AUTOCONNECT=true
ZSH_TMUX_AUTOQUIT=true
source $ZSH/oh-my-zsh.sh

export LANG=en_US.UTF-8

if [[ -n $SSH_CONNECTION ]]; then
    export EDITOR='vim'
else
    export EDITOR='MVIM'
fi

export ARCHFLAGS="-arch x86_64"

alias ~pf="cd ~/scripts/python/finance"
alias ~data="cd ~/data/sqlite3"
alias gem="git commit --allow-empty-message"
# alias ~local="PATH=$PATH:~/.local/bin"
